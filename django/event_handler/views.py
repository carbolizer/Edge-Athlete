"""
views.py — HTTP endpoints for the event_handler app.

Gutted at Phase 2: the old Device/MotionEvent motion-detection endpoints are
removed so the app imports cleanly against the new seven-model schema. The real
Edge Athlete REST API — nodes, racks (register + racknumber poll), athletes,
programs, sessions, sets, and the batch set-complete write — is built in
Phase 4. See SPEC.md → "REST API".
"""

# Endpoints are added in Phase 4. Intentionally empty for now.
