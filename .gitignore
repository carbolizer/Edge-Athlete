# macOS
.DS_Store
._*
.env


# django
venv/
__pycache__/
*.pyc
*.pyo
db.sqlite3


#react
# -- dependencies
/node_modules
/.pnp
.pnp.js

# -- testing
/coverage

# -- production
/build
# The React container builds its own bundle at image build time (see the `react`
# service in docker-compose.yml), so a committed dist is never the thing that
# gets served — it was only ever diff noise that churned on every build.
react/dist/

# -- misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
Sprint 1.html
npm-debug.log*
yarn-debug.log*
yarn-error.log*
docs/_build/

.obsidian/
