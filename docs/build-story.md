# How the build actually went

:::{note}
**Placeholder — written in step 3.**
:::

**What lands here:** The chronological narrative: the merge (P0–P15), then Sprint 1 through Sprint 3 phase by phase, what each phase was for, and what changed course along the way. The story, not the lookup.

**Built from:** `_SPEC.md` ("The build, in the order it actually happened"), git history on `main`
